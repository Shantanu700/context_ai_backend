// Mirrors certifier_reloaded_backend: the Python tooling is installed inside the
// container, so go-to-definition works against the interpreter that holds the deps.
{
	"name": "Context-Aware Ad Recommender (Django 6, Python 3.13)",
	"dockerComposeFile": [
		"../docker/nonprod/docker-compose.yaml"
	],
	"service": "context_ai_django",
	"workspaceFolder": "/backend",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-python.vscode-pylance",
				"batisteo.vscode-django",
				"eamodio.gitlens",
				"ms-azuretools.vscode-docker",
				"Anthropic.claude-code"
			],
			"settings": {
				// deps live in the system prefix, not a .venv — see UV_PROJECT_ENVIRONMENT
				"python.defaultInterpreterPath": "/usr/local/bin/python",
				"terminal.integrated.profiles.linux": {
					"zsh": {
						"path": "/usr/bin/zsh"
					}
				},
				"terminal.integrated.defaultProfile.linux": "zsh"
			}
		}
	}
}
